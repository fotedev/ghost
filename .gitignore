# IDE config directories
.cursor/
.windsurf/
.codeium/

# Legacy/archived script versions — local-only rollback copies, never committed.
# Versioning lives in Git tags + CHANGELOG.md.
archive/

# Backup files
*.backup
*.bak
*~
*.tmp
ID_Backups/

# Logs
*.log

# Python caching
__pycache__/
*.pyc

# Local state / fingerprint history
.local/state/LicenseIdentity/

# macOS
.DS_Store

# Test scratch file
test.txt

# Installers
CursorFreeVIP_1.11.03_windows.exe
CursorFreeVIP_1.8.08_windows.exe

# Scratch / Temp files
arena.txt
compare.md
desktop.ini
files.txt
structure.txt
models/
nul
/context_output
/.qoder
IMPLEMENTATION_PLAN.md
IMPLEMENTATION_GUIDE.md
WALKTHROUGH.md
/.context/inputs
Analyze Device Fingerprint Persistence.md
$outFile
.zcode/plans

# QoderWork research artifacts (extracted app manifest + Spec-Kit scaffold;
# not part of this automation suite)
package.json
my-project/

# Research specs may embed live device fingerprints / API keys — never commit
specs/

# One-off audit scratch (re-run the audit script instead of keeping output)
_ast_audit_zcode.ps1
_qoder_ast_report.md
_hkcr_search.ps1
_hkcr_search2.ps1

# Live hosts-file firewall tool — keep local-only until explicitly reviewed
block_qoder_domains.ps1

# Local trash — moved-aside junk, exes, research with secrets. Never commit.
.trash/

# Local env — tokens, secrets, machine-specific values. Never commit.
.envlocal

# Local-only: agent context/settings (per-machine) — never commit.
.context/

# Local-only: hosts-file firewall tool (security-sensitive) — never commit.
tools/block_qoder_domains.ps1
